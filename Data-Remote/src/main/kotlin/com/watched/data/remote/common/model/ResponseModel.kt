package com.watched.data.remote.common.model

/**
 * A model that represents a response from an external API.
 */
interface ResponseModel

/**
 * A model that represents a request to an external API.
 */
interface RequestModel
